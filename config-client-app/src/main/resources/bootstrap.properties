# 36 - defining the name 
spring.application.name=config-client-app

# 37 -  property to tell config client to find the config server via service discovery
spring.cloud.config.discovery.enabled=true

# 38 - defining the location of the discovery server 
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

